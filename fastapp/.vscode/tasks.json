{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Run fastapp server",
      "type": "shell",
      "command": "uvicorn fastapp.main:app --reload",
      "options": {
        "cwd": "${workspaceFolder}"
      },
      "group": {
        "kind": "build",
        "isDefault": true
      }
    }
  ]
}
