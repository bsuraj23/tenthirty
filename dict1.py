In Python, you can work with directories (folders) using the built-in os and pathlib modules. 

| command                   | Description                                          | Example                       |
| ------------------------- | ---------------------------------------------------- | ----------------------------- |
| `dict.get(key, default)`  | Get value for key, return `default` if key not found | `my_dict.get("age", 0)`       |
| `dict.keys()`             | Get all keys                                         | `my_dict.keys()`              |
| `dict.values()`           | Get all values                                       | `my_dict.values()`            |
| `dict.items()`            | Get all key-value pairs                              | `my_dict.items()`             |
| `dict.update(other_dict)` | Add/update entries from another dict                 | `my_dict.update({"age": 26})` |
| `dict.pop(key)`           | Remove and return item with key                      | `my_dict.pop("age")`          |
| `dict.popitem()`          | Remove and return last inserted key-value pair       | `my_dict.popitem()`           |
| `dict.clear()`            | Remove all items                                     | `my_dict.clear()`             |
| `dict.copy()`             | Make a shallow copy                                  | `new_dict = my_dict.copy()`   |
| `key in dict`             | Check if key exists                                  | `"name" in my_dict`           |
